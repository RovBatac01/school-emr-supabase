import { useNavigate } from 'react-router-dom';import { ShieldX } from 'lucide-react';import { Button, Card } from '../components/UI';export default function UnauthorizedPage(){const navigate=useNavigate();return <div className="grid min-h-[65vh] place-items-center"><Card className="max-w-lg p-8 text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-red-100 text-red-600 dark:bg-red-950"><ShieldX className="h-8 w-8"/></div><h2 className="mt-5 text-2xl font-bold">Access restricted</h2><p className="mt-2 text-sm leading-6 text-slate-500">Your assigned role does not permit access to this module. The same restriction is enforced by PostgreSQL Row Level Security.</p><Button className="mt-5" onClick={()=>navigate('/dashboard')}>Return to Dashboard</Button></Card></div>;}
