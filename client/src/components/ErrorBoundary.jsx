import React from 'react';import { AlertTriangle } from 'lucide-react';import { Button, Card } from './UI';export default class ErrorBoundary extends React.Component{constructor(p){super(p);this.state={error:null};}static getDerivedStateFromError(error){return{error};}componentDidCatch(error,info){console.error(error,info);}render(){if(this.state.error)return <div className="grid min-h-screen place-items-center bg-slate-50 p-5 dark:bg-slate-950"><Card className="max-w-xl p-8 text-center"><AlertTriangle className="mx-auto h-12 w-12 text-red-500"/><h1 className="mt-4 text-2xl font-bold">The application encountered an error</h1><p className="mt-2 text-sm text-slate-500">No raw database information was displayed. Reload the page and review the browser console for development details.</p><Button className="mt-5" onClick={()=>location.reload()}>Reload Application</Button></Card></div>;return this.props.children;}}
